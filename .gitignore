# Dependencias y temporales
node_modules/
.smoke-out/
*.log

# Archivos locales de configuración que no deben subirse
github-token.txt
.env
clave-gemini.txt

# Workflow de automatización (se reactiva cuando el token tenga scope "workflow")
.github/
